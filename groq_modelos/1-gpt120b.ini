[groq]
model = "openai/gpt-oss-120b"
temperature = 0.0
top_p = 1
max_tokens = 2048
; reasoning_effort = "high"     # "", "low", "medium", "high"
json_mode = true          # força JSON (response_format)
stop = []                 # ou ["</relatorio>"]
